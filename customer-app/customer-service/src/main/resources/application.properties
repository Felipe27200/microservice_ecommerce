spring.application.name=customer-service

# MySQL Database Connection
spring.datasource.url=${DATASOURCE_URL:jdbc:mysql://localhost:3308/ecommerce_customers?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATASOURCE_USER:root}
spring.datasource.password=${DATASOURCE_PASSWORD:root123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

apiPrefix=api
